{
  "name": "@reknowable/db-tests",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit",
    "test": "echo 'db-tests excluded from workspace test; run `pnpm db:test`'",
    "test:fast": "echo 'db-tests excluded from workspace test:fast'",
    "db:test": "vitest run"
  },
  "dependencies": {
    "@reknowable/test-utils": "workspace:*",
    "@reknowable/types": "workspace:*",
    "@supabase/supabase-js": "^2.50.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
