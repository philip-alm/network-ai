/**
 * @network-ai/ui — leaf UI components shared across web + native.
 *
 * Pure presentational primitives. Anything that needs state, data, or business
 * logic belongs in @network-ai/app/features/, not here.
 */

// Components will be added in Phase 6.
export {};
