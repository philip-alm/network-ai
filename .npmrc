# Strict no-hoist: keeps Expo + Next.js from fighting over duplicated react / react-native versions.
# This is the T3 Turbo canonical config — do not relax without understanding why.
public-hoist-pattern[]=""
shamefully-hoist=false
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
